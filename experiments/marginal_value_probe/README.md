# Frozen Marginal-Value Probe

Phase B compares parameter-matched local and set-aware probes on exact
counterfactual marginal utilities extracted from frozen base checkpoints.

```bash
/opt/conda/bin/python experiments/marginal_value_probe/run.py
```

Smoke test:

```bash
/opt/conda/bin/python experiments/marginal_value_probe/run.py --domain rag --examples 24
```

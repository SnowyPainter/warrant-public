# Permission decomposition

Architecture-matched CTDG factorial experiment for the reviewer questions:
path exposure versus learned permission, item-wise versus scalar magnitude,
renormalized attention reweighting, and interaction with generic attention
Warrant.

```bash
/opt/conda/bin/python experiments/path_localization/run.py \
  --config experiments/permission_decomposition/config.yaml
```

Results are isolated under `experiments/permission_decomposition/outputs`.
